{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build with clang",
            "type": "shell",
            "command": "clang",
            "args": [
                "-g",
                "-Wall",
                "-Wextra",
                "${file}",
                "-o",
                "${fileDirname}/${fileBasenameNoExtension}"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"]
        }
    ]
}